
def the_brand(vehicle_obj, brand):
    vehicle_obj.brand = brand              # obj_name.main_method overwrite initial definition in main class


def the_color(vehicle_obj, color):
    vehicle_obj.color = color


def the_origin_place(vehicle_obj, place):
    vehicle_obj.origin_place = place


def the_year(vehicle_obj, year):
    vehicle_obj.year = year


def the_price(vehicle_obj, price):
    vehicle_obj.price = price
